version: '3.8'

services:
  geolocation:
    build: .
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GOOGLE_MAPS_API_KEY=${GOOGLE_MAPS_API_KEY}
    volumes:
      - ./runs:/app/runs
      - ./analysis:/app/analysis
    command: python3 run_experiment.py --help 